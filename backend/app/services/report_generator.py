"""Streaming report generation - used by the download endpoint."""

# The streaming CSV logic is directly in the results endpoint (results.py).
# This module is reserved for future Excel export or PDF generation.
